% visualizar pixeles que detectan las esferas

% Crear Ib con esferas multiples

% Ib_multiples_esferas = calcula_deteccion_multiples_esferas_en_imagen(I,datosMultiplesEsferas(:,4),datosMultiplesEsferas(:,1:3));
% Ib = bwareaopen(Ib_multiples_esferas, numPix);

% En funcion visualiza, poner los pixeles de Ib de color rojo
% Io = funcion_visualiza(I, Ib, rojo, false);
    


% px de las esferas sujetos a movimiento
    % Ib de pixeles dentro de esferas combinada con un and con la de movimiento


% si no detecta centroide: poner en izquierda superior izquierda (por ej)
% HECHO .> if not centroides, else: poner en la izq


% no eliminar componentes conexas
% wtf is this
